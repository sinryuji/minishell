# minishell
Simple POSIX Shell.

### Usage
```shell
make && ./minishell
```

### Supported
- redirection(`<`, `<<`, `>`, `>>`)
- pipe(`|`)
- `&&`, `||`

### Built-in functions
- echo
- cd
- pwd
- export
- unset
- env
- exit
